import {
  "moonbitlang/core/env",
  "moonbitlang/core/strconv",
  "moonbitlang/core/string",
  "moonbitlang/async",
  "moonbitlang/async/http" @async_http,
  "moonbitlang/async/socket" @async_socket,
  "moonbitlang/async/io" @async_io,
  "trkbt10/vcdb/http",
  "trkbt10/vcdb/core/storage",
}

import {
  "moonbitlang/core/test",
} for "wbtest"

warnings = "-unused_package"

options(
  "is-main": true,
  targets: {
    "main.mbt": [ "native" ],
    "main_stub.mbt": [ "wasm", "wasm-gc", "js" ],
    "native_serve_wbtest.mbt": [ "native" ],
  },
)
